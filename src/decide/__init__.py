"""Settle — Layer 5 decision & action package.

Deterministic policy gate (auto vs escalate) + action selection + payable + a
finance-handoff STUB. The LLM may only draft a human-readable cover note from facts
already in the structured packet — it never decides routing and never emits or alters
a number.
"""
